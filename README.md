# Address-Book-using-C-Programming
The Address Book application using C Programming performs CRUD operations Create, Read, Update and Delete on contact data. It persistently stores the data in CSV format using file handling.
